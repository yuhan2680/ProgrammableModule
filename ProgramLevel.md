# 游戏关卡（编程模式）

## 第一关

**解锁新模块**：移动模块

**解锁新指令**：move(角度,单位)

**模块数量**：1

**关卡目标**：移动到终点

**教程**：向玩家介绍移动模块和move指令，介绍move指令的两个参数：角度和距离。

**关卡内容**：一个“S”形路径，需要控制机器人按照以下路径行动以到达终点。

* 向0°移动4单位
* 向90°移动2单位
* 向180°移动4单位
* 向90°移动4单位
* 向0°移动4单位

## 第二关

**模块数量**：2

**关卡目标**：移动到终点

**教程**：告诉玩家有关“模块数量”限制的信息，并指出相同模块叠加可以叠加效果（N个移动模块时移动速度是单个模块的N倍），让玩家叠加两个移动模块。

**关卡内容**：一个向右的直线，但是有一个闸门，只有玩家叠加两个模块才有足够的速度在闸门关闭前冲向终点。

## 第三关

**解锁新模块**：近战模块

**解锁新指令**：attack(角度)

**模块数量**：2

**关卡目标**：攻击障碍物

**教程**：介绍attack指令，并指出attack只能攻击到周围两个单位的物品。同时指出每个模块占地0.5x0.5单位，因此需注意模块排布。

**关卡内容**：玩家右侧5单位处有个障碍物，玩家需要移动到那里并攻击该障碍物。

## 第四关

**解锁新模块**：射击模块

**解锁新指令**：shoot(角度), time.wait(tick数)

**模块数量**：1

**关卡目标**：击毁敌人

**教程**：
* 介绍射击指令，指出子弹会在射击后不断减速，并最终停下消失。敌人受到的伤害与子弹速度成正比。
* 同时，近战模块一次只能攻击一个模块，所以在此处若使用近战模块攻击敌人只会摧毁其移动模块，故鼓励玩家使用射击模块击毁敌人。
* 介绍模块的cd（冷却时间）内，再次调用模块是没反应的
* 让玩家在等到敌人进入可击杀距离之后再进行击杀。

**关卡内容**：敌人在玩家右侧6单位处，安装了移动模块和近战模块。敌人会在向左移动4.5单位后使用attack指令。

## 第五关

（第五关设计可能要改一下，毕竟同时攻击也行）

**模块数量**：3

**关卡目标**：在不被警卫杀死的情况下逃离监狱

**教程**：让玩家破坏警报器和警卫，先破坏左边的警卫再破坏右边的警报器。并介绍有多个相同模块时，如果直接调用attack会同时调用所有模块，给模块命名并按照xx.attack()的方式进行攻击则会单独只调用一个模块的攻击。


**关卡内容**：玩家被困在监狱里了，该机器左边2单位处有警卫，右边2单位处有警报器。先破坏警报器则会被警卫杀死。破坏完警卫和警报器后向上移动可以逃出出口。

## 第六关

**模块数量**：1

**关卡目标**：移动到终点

**教程**：介绍loop{}的用法，通过循环向右移动3和向上移动2到达终点。

**关卡内容**：一个阶梯状关卡，由重复十次的3x2阶梯组成。

## 第七关

**模块数量**：2

**关卡目标**：击败敌人

**教程**：使用if{}else{}，在射击模块冷却完之后射击，其余时间后退

**关卡内容**：一个横向的狭长通道，敌人在玩家右侧6格处。右侧的敌人不断往左逼近，并不断地在往左边攻击。玩家需要边往左边后退边向右边射击。

## 第八关

**模块数量**：3

**关卡目标**：在不触发警报器的情况下逃离监狱

**教程**：让玩家同时破坏两个警报器，并介绍simultaneously{}函数可以同时执行多个函数。

**关卡内容**：玩家被困在监狱里了，该机器左边2单位、右边3单位处均有警报器。移动会拉响警报，破坏一个警报器时另一个警报器会被拉响。玩家需同时破坏两个警报器并向上走逃出出口即可胜利。

## 第九关

**解锁新模块**：测距模块

**解锁新指令**：distance(角度)

**模块数量**：2

**关卡目标**：走到终点

**教程**：玩家需要通过测距模块判断前方通道的距离，并通过向左右走绕过通道自动寻路走到终点。

**关卡内容**：一条长蛇形隧道。

## 第十关

**解锁新指令**：constant 常量、variable 变量

**关卡内容**： 限制三个模块，_（用雷达+移动+远程）_ 攻击一个随机移动没有规律的敌人,要求追着攻击并击败。

## 第十一关

**解锁新指令**：function 函数

**模块数量**：2

**关卡目标**：躲过5次攻击

**教程**：敌人的行动是固定的，只需要每次检测到敌人出现在自己面前时移动走就可以了，最好用函数封装起来。

**关卡内容**：敌人有三个模块：移动模块、攻击模块和雷达模块。会通过雷达锁定玩家位置，移动到玩家面前一格，随后马上攻击

## 第十二关

**解锁新指令**：for

**模块数量**：2

**关卡目标**：摧毁所有敌人

**教程**：使用for循环不断用测距模块对八个方向进行扫描，检测到距离异常后用设计模块开枪。

**关卡内容**：敌人会从八个方向袭来，每次一个方向的敌人出现时摧毁它即可。

## 第十三关

**解锁新模块**：雷达模块

**解锁新指令**：scan()，返回最近一个敌人，用scan.Angle()和scan().Position就可以获取敌人的角度和距离。如没有敌人，则返回Null。

**模块数量**：2

**关卡目标**：摧毁所有敌人

**教程**：使用雷达模块的scan扫描到敌人之后，用设计模块往那个方向开枪吧。

**关卡内容**：敌人不再是从八个方向袭来了，而是随机从所有方向袭来。

## 第十四关

**模块数量**：2

**关卡目标**：摧毁所有敌人

**教程**：一边向前移动，一边扫描敌人。扫描到敌人之后向敌人的方向开枪。

**关卡内容**：一条长长的道路上，有很多往下的被铁栅栏封住的通道，敌人可能从下面冒出来。铁栅栏机器无法通过，只有子弹和攻击物品可以通过。

## 第十五关

**解锁新指令**：解锁事件radar.onDetected{EnemyPosition -> 变量}

**模块数量**：2

**关卡目标**：摧毁所有敌人

**教程**：通过雷达的“检测到敌人”事件来取消不断扫描的过程。敌人位置会被赋值给->后连接的变量。

**关卡内容**：一条长长的道路上，有很多往下的被铁栅栏封住的通道，敌人可能从下面冒出来。铁栅栏机器无法通过，只有子弹和攻击物品可以通过。
