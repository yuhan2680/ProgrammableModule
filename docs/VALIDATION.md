# 验证记录

## 2026-09-23：窗口预设与最大化

- 设置「显示」首行加入六档16:10分辨率和最大化，默认1280×800，三种界面语言均覆盖。项目启动与运行窗口禁止自由拖动边缘，canvas_items/expand 保持等比并扩展可见区域。
- Godot 4.7.2 macOS：新增设置模型回归168项、窗口界面headless回归98项、实际OpenGL窗口回归161项，全部零失败。实际输入覆盖分辨率选择、禁用尺寸、三语、1024×640和1280×800、最大化后回到固定尺寸、同帧快速改选及重建游戏恢复设置。GPU验证2:1客户区四边为背景色，已目视检查模糊背景铺满、原生最大化和小窗口布局。
- 既有设置303项、开放设置布局246项、启动加载133项、游戏界面1200项全部零失败。启动测试在4.7.2退出时报告9个RefCounted对象的既有清理警告；在本次修改前的隔离工程复测得到同样警告。其余最终专项日志无解析、脚本或引擎错误。
- 屏幕尺寸筛选覆盖恰好容纳1280×800及小于最低预设的情况；正常窗口边框尺寸单独缓存，显示器变化只修正越界位置。测试不读写正式用户设置、地图或通关记录。
- Windows采用相同Godot窗口API，本机未进行Windows原生窗口验证；本次未重新分发导出包，正式使用需从更新后的工程重新导出。

## 2026-09-19：第十五关与雷达事件

- 直接读取最新 ProgramLevel.md，并按用户确认使用移动、雷达、射击三个模块。第十五关沿用四格主通道与栅栏支道，十波敌方移动/近战模块各有 2.8 耐久，限时 240 秒，整机残骸约三秒淡出。
- Godot 4.5.1 完成 `tools/test.ps1` 清单中的全部 62 组回归。首次执行发现新增关卡影响的旧目录数量、缩略图、排序与导入卡索引断言；修正后复测受影响用例并续跑剩余清单。最终各组均为零失败，最终有效日志无引擎错误或警告；初次执行日志保留供追溯。
- 新增雷达事件语法 117 项、运行时 102 项、编辑辅助 210 项和第十五关集成 507 项检查通过。覆盖独立权限、声明顺序、固定事件结构、真实雷达能力、无目标清空、全局绑定、快照隔离、长动作期间刷新、暂停、停止与重试。
- 正式关卡用三模块及不调用 `scan()` 的事件程序实际完成十波，验证栅栏、触发区域、逐模块损毁、2.8 耐久与残骸淡出；逐步代码提示同样能生成可通关程序。前十四关保持事件权限关闭，导入同 ID 地图不获得教学代码提示。
- 游戏界面 1198 项、关卡搜索 670 项及布局回归通过。实际 GPU 视口分别检查中英文第十五关地图、雷达事件代码与实时状态显示；深浅配色、Tab 补全及指令目录另有专项覆盖。修改的 GDScript 中 584 个顶层函数声明均有中文用途注释。
- 测试在独立项目副本和独立用户目录进行；仅同步验证过的代码、数据、测试与文档，不同步测试 `override.cfg`、引擎缓存或测试存档。同步前检查原文件哈希并备份原文件。

## 2026-09-17：玩家随机函数

- 第十关独立开放 `random()` 与 `randomInt(a, b)`，第十一关继承；前九关和旧地图缺省关闭。新增 `allow_random`、显式随机表达式 AST 和运行器独立随机源；同步教程、指令 JSON、Tab 补全、语法高亮和中英文说明。
- Godot 4.5.1 执行 `tools/test.ps1`：47 组回归全部通过，无 ERROR、SCRIPT ERROR、Parse Error 或 WARNING。新增随机语言 494 项、随机关卡 81 项；补全 464 项、指令资料 641 项、游戏界面 1079 项、布局 3285 项均为零失败。
- 随机专项覆盖闭区间和完整 signed32 整数范围、相同端点、嵌套及动态端点、非法参数行列、常变量/条件/函数/tick/并发求值、静态校验与未执行分支不抽样、计算预算、取消、暂停和重试。正式会话验证玩家随机调用不会扰动固定测试种子的敌方路线；种子不写入正式关卡。
- 第十关追猎 447 项继续通过，包括 64 种敌人随机路线各运行两种程序的 128 次真实通关；JSON 比对确认地图布局、实际耐久、时限、装配限制、目标和初始程序保持原值，仅新增随机解锁与一页说明。
- macOS 沙箱测试使用独立测试存储目录与独立引擎配置；本地 CA 证书包解决沙箱内系统证书读取问题。测试用 `override.cfg` 只存在于隔离副本，不同步到正式工程，不触及玩家存档。


## 2026-09-10：底层与地图编辑器

环境：Windows，本地项目直接运行。以下记录保留首次底层交付时的实际结果。

| 环境 / 检查 | 结果 |
| --- | --- |
| Godot 4.5.1 stable mono：导入 + 底层回归 | 21 个用例，143 项断言，0 失败 |
| Godot 4.5.1：编辑器场景冒烟 | 21 项检查，0 失败 |
| Godot 4.7.2 stable Steam：导入 + 底层回归 | 21 个用例，143 项断言，0 失败 |
| Godot 4.7.2：编辑器场景冒烟 | 21 项检查，0 失败 |
| Godot 4.5.1 / OpenGL：真实视口渲染 | 22 项检查，0 失败；已目视检查截图 |
| GDScript 函数注释 | 175 个函数均有用途注释 |
| 示例数据与 Schema 文件 | JSON 语法检查通过 |

最终运行日志没有 GDScript 解析、编译或运行错误。测试命令见 README 与 tools/test.ps1，脚本要求 PowerShell 7，支持控制台版和 GUI 版 Godot。

底层测试包括：错误类型、未知 ID、重复 ID/坐标、资源路径、格式版本、保存失败保护、扩展字段往返、编辑撤销重做、固定 tick、速度叠加、方向、精确距离、取消、忙碌命令、完整出生占地、高速穿洞、对角碰撞、极大有限数值、第二台机器的 void 限制以及结算后信号通知。

编辑器测试加载真实场景，通过控件及画布信号验证绘制、擦除、撤销、重做、元数据编辑、文件保存载入、未保存保护、失败载入保留原文档、试玩与重置隔离。图形检查截图保存在 test-results/editor.png；该目录为本机验证产物。

JSON Schema 用作内容编写辅助，动态引用与占地校验由 GDScript 执行；该次未运行外部 JSON Schema 验证器。该次没有进行导出包、正式 UI、战斗、DSL 或多人测试。

## 2026-09-11：第一关与玩家程序流程

环境：Windows，Godot 4.5.1 stable mono 与 Godot 4.7.2 stable Steam。两套引擎均完整运行导入和以下五套测试，每套引擎合计 439 项断言或检查，全部通过。

| 检查 | Godot 4.5.1 | Godot 4.7.2 Steam |
| --- | --- | --- |
| 底层回归：21 个用例 | 143 项断言，0 失败 | 143 项断言，0 失败 |
| 地图编辑器场景冒烟 | 21 项检查，0 失败 | 21 项检查，0 失败 |
| 语言 Lexer / Parser / AST / Runner | 112 项检查，0 失败 | 112 项检查，0 失败 |
| 关卡、装配、草稿与游戏会话：16 个用例 | 113 项断言，0 失败 | 113 项断言，0 失败 |
| 实际游戏 UI 场景冒烟 | 50 项检查，0 失败 | 50 项检查，0 失败 |

另外使用 Godot 4.5.1 / OpenGL 运行实际视口检查，55 项检查全部通过，标准错误输出为空。这 55 项包含相同 UI 流程及 5 项截图检查，不再与上表重复累加。已目视检查关卡选择、导入提示、程序、组装与通关五张截图，第一关完整地图在工作台可见；本机产物位于 `test-results/level_select.png`、`level_import.png`、`level_program.png`、`level_assembly.png`、`level_complete.png`。

两套引擎最终日志均没有引擎 `ERROR`、GDScript 解析、编译或运行错误。`tools/test.ps1` 同时检查退出码、完成汇总、`SCRIPT ERROR` 及行首一般 `ERROR`；Godot 可能在用例中断后仍返回 0，因此单看退出码或“0 项失败”不足以判定通过。

静态检查确认 `scripts/` 与 `tests/` 中的 375 个函数全部具有前置用途注释，`data/` 与 `schemas/` 内所有 JSON 均通过语法检查。本次仍未运行独立 JSON Schema 验证器；动态引用、装配与地形规则由 GDScript 测试验证。

游戏层与 UI 测试分别使用独占的 `user://tests/gameplay_*`、`user://tests/game_ui_*` 目录，并在正常结束后清理，不写入真实 `user://solutions`。UI 测试注入 `folder_opener` 记录调用，验证导入按钮提交了正确目录的打开请求，不启动真实资源管理器。

语言检查覆盖带行列信息的 Token、显式 AST、唯一 `main()`、正负数与小数、注释与 CRLF、调用间换行、未解锁语法、输入上限、整程序参数预检、顺序移动、零距离、阻挡、取消和新世界重试，还覆盖同步行号通知与 tick 通知触发的重入保护。

游戏层检查包括：

- 首关仅一个移动模块，起始模板没有完整答案；独立于语言执行五段设计路线，精确到达 `(5.5, 1.5)`。
- 关卡元数据类型、模块引用、终点范围；无目标地图导入为沙盒；目录中坏文件和重复 ID 不隐藏其他有效关卡。
- 装配数量、半格偏移、范围、重叠、重复或非法模块名；失败修改不提交；运行文档与原关卡隔离；出生占地不能悬空。
- 中文源程序与装配保存往返、缺失或损坏草稿、关卡 ID 哈希文件名，以及修改草稿不清除通关记录。
- 通过真实语言入口运行第一关并只发送一次完成通知；无目标沙盒正常执行；高速单 tick 经过小终点仍通关；缺少 `main()`、程序结束未抵达目标、void 阻挡的失败反馈；暂停冻结 tick，恢复继续，重置保留程序与装配。

实际 UI 场景检查通过真实控件和画布信号执行关卡选择、加号导入、程序编辑、错误行高亮、放置拖动与命名模块、运行、暂停、通关、返回及草稿恢复，并验证旧地图编辑器入口和未保存保护。

草稿恢复回归专门覆盖不兼容装配和损坏 JSON：没有编辑便返回时保留原文件；首次编辑后的自动保存和未编辑时主动保存都先生成同目录 `.recovery.*.json` 副本，逐字保留原文；后续普通保存不重复备份，也不清除通关记录。恢复提示与教程的弹窗互斥错误已在集成时发现、修复并通过引擎错误检查。运行与暂停期间的运行快捷键不能重置时间累加器、替换世界或解释器实例。

独立入口包含 `tests/language_runner.gd`、`tests/gameplay_runner.gd` 与 `tests/game_ui_smoke.gd`。首次运行前仍需导入全局类；完整命令由 `tools/test.ps1` 统一维护。本次没有制作或验证导出包，也没有进行战斗、完整语言或多人测试。

## 2026-09-11：主菜单、空白组装与设置

本轮在上一轮交付基础上调整游戏页面流程，并增加设置模型与面板。Windows 上的 Godot 4.5.1 stable mono 和 Godot 4.7.2 stable Steam 均完成工程导入及六套测试，每个引擎合计 616 项断言或检查，全部通过。

| 检查 | Godot 4.5.1 | Godot 4.7.2 Steam |
| --- | --- | --- |
| 底层回归：21 个用例 | 143 项断言，0 失败 | 143 项断言，0 失败 |
| 地图编辑器场景冒烟 | 21 项检查，0 失败 | 21 项检查，0 失败 |
| 语言 Lexer / Parser / AST / Runner | 112 项检查，0 失败 | 112 项检查，0 失败 |
| 关卡、装配、草稿与游戏会话：20 个用例 | 159 项断言，0 失败 | 159 项断言，0 失败 |
| 设置模型与设置面板 | 99 项检查，0 失败 | 99 项检查，0 失败 |
| 实际游戏 UI 场景冒烟 | 82 项检查，0 失败 | 82 项检查，0 失败 |

两套引擎的最终日志没有一般引擎 `ERROR`、`SCRIPT ERROR`、解析或编译错误。统一入口仍是 `tools/test.ps1`，包含退出码、日志及完成标记检查。新增 `tests/settings_runner.gd` 使用独占测试路径，音量与语言测试结束后恢复运行环境，避免污染真实玩家设置。

Godot 4.5.1 / OpenGL 的最终实际视口测试另有 92 项检查通过、标准错误输出为空，其中包括上述 82 项 UI 流程检查及 10 项非空截图检查，不与无窗口结果重复累计。10 张截图输出到 `test-results/`；已目视检查中英文主菜单、英文设置、空白组装准备、全宽英文组装、程序与完整地图等主要画面。

本轮验证覆盖：

- 启动主菜单的开始、地图编辑器、设置、离开四个入口；关卡页不再提供地图编辑器入口。页面重新获得焦点时，选关页刷新目录，设置页保持当前页面。
- 每次进入关卡都建立空装配；地图出生模板仍保留给底层格式与地图编辑器。新会话之间不共享布局，空装配不能确认或启动运行。
- 组装目录显示全部注册模块，未解锁类型保留可见但禁用；满上限时目录与画布新增入口都禁用。直接调用和同步 `changed` 回调中的重入新增也不能绕过模型限制。
- 明确放置合法模块后才能进入编程；悬空装配不能确认。程序页内切换组装时隐藏地图，让模块目录和画布使用整行宽度；运行时恢复地图与代码观察区。
- 源程序自动恢复，旧布局只有主动点击“载入上次装配”才安装。空组装页未编辑便返回不会清空合法历史布局；损坏或不兼容草稿的原文保留与恢复备份检查继续通过。
- 主音量立即应用到 Master 总线，零值静音；简体中文与 English 即时切换并重排原生控件。有效设置保存往返、非法值拒绝、损坏文件保留、未知字段保留和写盘失败反馈均有回归覆盖。
- 既有程序行高亮、暂停与恢复、快捷键锁定、第一关通关、草稿与进度隔离、编辑器未保存保护继续通过。

设置使用 `user://settings.json`，结构为 `{"format_version":1,"audio":{"master_volume":1.0},"interface":{"language":"zh_CN"}}`。测试注入独占的设置、草稿与关卡目录；UI 的 `folder_opener` 和 `quit_handler` 均使用替身记录调用，验证正确的文件夹与退出请求，不启动资源管理器或执行真实系统退出动作。

清理仅用于旧测试的工作台代理后，最终 `scripts/` 与 `tests/` 中共有 440 个函数，全部具有前置用途注释；`data/` 与 `schemas/` 中所有 JSON 语法检查通过。没有生成或验证导出包，本轮也没有增加战斗、完整语言或多人验证。

## 2026-09-11：编程页返回组装

编程工作台新增“编辑模块”按钮，直接返回独立组装页面并保留同一会话的程序和布局，避免重新进入关卡导致空装配。运行或暂停时返回会取消当前动作；确认修改后回到编程页。新增函数包含中文用途与生命周期说明，按钮与提示包含中英翻译。

Godot 4.5.1 与 Steam 4.7.2 均通过 `tools/test.ps1` 六套检查：底层 143、地图编辑器 21、语言 112、玩法 159、设置 99、游戏 UI 133，每轮共 667 项断言或检查，日志无引擎或脚本错误。新增 UI 回归覆盖编辑、运行、暂停与语法失败四种状态，验证实际按钮调用、未同步和非法源码保留、旧磁盘草稿不回读、同一会话与装配对象、旧动作取消、修改模块后确认返回编程。原“← 关卡”按钮仍直接返回选关页。

Godot 4.5.1 / OpenGL 实际视口检查为 144 项，包含 133 项流程和 11 项截图检查，标准错误输出为空。已目视确认编程页顶部入口以及返回组装后的现有模块与确认按钮；截图为 `test-results/level_program.png`、`test-results/return_to_assembly.png`。本次没有进行导出包验证。

## 2026-09-11：装配中心与共边连接

首个模块必须在 `(0,0)`，后续模块可以与任意已有模块共边并形成分支。删除中心或桥接模块后仍可保存草稿，确认及运行要求中心存在且所有模块沿边连通。确认按钮和运行按钮实时显示禁用原因；缺中心与断连提示支持即时中英切换。

Godot 4.5.1 与 Steam 4.7.2 的六套回归均通过：底层 143、地图编辑器 21、语言 112、玩法 239、设置 99、游戏 UI 169，共 783 项断言或检查，日志无引擎或脚本错误。4.5.1 全套检查后补充了 3 项短边接触断言，并单独重跑玩法套通过；4.7.2 全套包含最终补充。新覆盖包括首个模块位置、单模块移动、分支、角点与间隙拒绝、删除中心与桥接后的修复、异尺寸与很短的正长度共边、旧草稿及直接调用不能绕过最终校验、缺中心草稿保存后重新进入并手动恢复，以及出生占地跨入 void。

Godot 4.5.1 / OpenGL 实际视口检查为 181 项，其中 12 项截图检查，标准错误输出为空。通过向游戏视口投递鼠标移动事件触发原生悬停，已目视确认禁用按钮仍显示缺中心原因（`test-results/assembly_missing_center.png`），并检查中英组装布局。多模块关卡仅为隔离测试夹具，内置关卡仍只有第一关。

静态检查确认 `scripts/` 与 `tests/` 的 457 个函数均有前置中文用途注释，`data/` 与 `schemas/` 中 8 个 JSON 文件语法有效。本次没有生成或验证导出包。

## 2026-09-11：地图编辑器使用正常关卡试玩流程

编辑器删除了角度、距离、直接执行 `move` 和局部停止/重置控件。「开始测试」将当前未保存地图转换为关卡快照，进入空装配，确认后进入现有编程工作台。新增 `1..256` 的模块数量上限，写入 `properties.level.module_limit` 并支持撤销、重做和保存。主菜单入口与独立 `map_editor.tscn` 共用同一流程。

Godot 4.5.1 与 Steam 4.7.2 均通过完整 `tools/test.ps1`：底层 160、地图编辑器 62、语言 112、玩法 239、设置 99、游戏 UI 172，每轮共 844 项断言或检查，日志无引擎或脚本错误。覆盖模块上限非法值拒绝、元数据保留、持久化、撤销重做、未失焦输入保存，以及同帧更新数值后开始测试不被旧显示文本覆盖。

编辑器集成回归通过真实控件进入组装、编程并完成测试目标，验证模块上限即时执行、组装与编程间保留同一会话、返回取消旧命令、原地图路径/未保存状态/撤销重做/滚动位置保持不变。再次开始测试采用最新地图与上限并建立空装配；缺出生点留在编辑页报告错误。测试还验证强制保存、计时保存和通关不能改写同 ID 正式草稿或完成记录，以及试玩中关闭窗口仍保护未保存地图。

Godot 4.5.1 / OpenGL 编辑器检查为 67 项，含 5 项真实视口截图检查，标准错误输出为空。已目视检查中英编辑器的数量上限与测试入口、试玩组装/编程页和返回页面；英文长提示换行后不再挤出窗口。截图位于 `test-results/editor.png`、`editor_english.png`、`editor_playtest_assembly.png`、`editor_playtest_program.png` 和 `editor_return.png`。测试图片中的扩展地图仅是隔离夹具。

静态检查确认 `scripts/` 与 `tests/` 的 470 个函数均有前置中文用途注释，8 个内容及 Schema JSON 语法有效。本次没有生成或验证导出包。

## 2026-09-11：圆角外观与三关重制（macOS）

环境：Godot 4.5.1 Standard，macOS。使用 `python3 tools/test.py --godot <引擎路径>` 运行现有六套与新增两套回归，共 **964 项断言或检查，0 失败**。Python 入口与 PowerShell 入口调用同一组 GDScript，均校验退出码、完成标记及引擎/脚本错误。

| 检查 | 结果 |
| --- | --- |
| 底层 | 160 项断言，0 失败 |
| 地图编辑器 | 62 项检查，0 失败 |
| 语言 | 112 项检查，0 失败 |
| 玩法、装配、草稿、会话 | 239 项断言，0 失败 |
| 设置 | 99 项检查，0 失败 |
| 游戏 UI 流程 | 172 项检查，0 失败 |
| 三关内容 | 60 项检查，0 失败 |
| 中英与窗口布局 | 60 项检查，0 失败 |

三关回归通过原有 `GameSession` / `AssemblyModel` / `SimulationWorld`，验证 S 路径 3+3 对齐、第二关单/双模块 80/40 tick、第三关双模块小数距离通关、错误转弯受阻、失败后修正重跑，以及运行不改写原关卡。新增内容只使用现有格式。

布局回归在 1280×800 与 1000×740 两种尺寸下分别检查中英文主菜单、装配、编程、设置和编辑器关键控件。实际视觉检查使用官方 Godot 4.5.1 WebGL 模板导出的同一工程副本，目视确认了主菜单、三关列表、教程弹窗、英文装配、英文地图编辑器和编程工作台；在浏览器实际点击运行并完成第三关，页面运行日志无错误。

逐字节比对了数据层、模块行为、模拟、语言、游戏会话、装配/存档/设置模型、编辑文档、Schema 与模块/地块定义，共 63 个受保护文件（包含脚本及配套文件），内容保持不变。新增函数附中文用途注释，内容 JSON 与 SVG 均通过语法检查。

本轮没有执行 PowerShell（当前 Mac 未安装），没有验证 Godot 4.7 或原生桌面发布包。历史 Windows 验证记录保留，其结果属于各自记录日期，不作为本轮测试结果。

写回 `/Users/mac/Desktop/ProgrammableModule` 后，在该实际路径重新完成导入与全部 964 项检查，仍为 0 失败。

## 2026-09-11：第二、三关设计修正

本轮使用 macOS Godot 4.5.1 Standard 与 PowerShell 7.6.6 执行 `tools/test.ps1`。8 套回归共 1118 项检查/断言，0 失败：底层 160、编辑器 62、语言 112、玩法 239、设置 99、游戏界面 200、三关 154、布局 92。检查引擎日志及各套完成标记。另通过 21 项 JSON Schema / SVG 校验。

新增验证覆盖单/双移动模块过闸、35 tick 时序、暂停、重置、落闸压住机器、高速扫掠、前两关攻击锁定、第三关近战预检、射程边界、方向、挥空、真实模块偏移、穿墙阻挡、伤害统一提交、取消及同 tick 重入。编辑器通过实际测试入口验证未保存的闸门/耐久配置、撤销重做、原文档与正式草稿隔离、JSON 文件往返和非法对象保存保护。

两种语言、1280×800 和 1000×740 的布局检查包含新关卡代码区、对象状态、运行按钮及地图视口。实际 Godot WebGL 导出副本目视验证第二关打开/关闭闸门、单驱动受阻，第三关 SVG 近战模块、障碍物和英文通关状态。视觉副本使用独立测试数据目录，不写真实玩家存档。

本轮未验证 Godot 4.7 原生 macOS 窗口；未更改用户 project.godot 的版本标记或渲染设置。SVG 均为项目内原创矢量资源，未引入新运行时插件。

## 2026-09-11：第四关「持续射击」

按 ProgramLevel.md 的第四关加入射击模块、逐关解锁的 `tick()` / `shoot(angle)`、减速弹道和接近后攻击的双模块敌人。敌人位于玩家右侧 6 单位，先向左移动 4.5 单位，再向左近战攻击。玩家只能安装 1 个模块，须击毁敌方全部模块；15 秒未完成则结束本次尝试，避免仅拆掉敌方移动模块后无限僵持。

macOS Godot 4.5.1 Standard 下，使用 PowerShell 统一入口 `tools/test.ps1` 完成 10 套回归，共 **1,483 项检查或断言，0 失败**：底层 160、地图编辑器 62、语言 172、玩法 239、设置 99、游戏界面 265、关卡与战斗 237、弹道碰撞 29、第四关 112、布局 108。最终日志没有引擎、脚本、解析或编译错误，也没有资源泄漏警告。

新增回归覆盖逐关语法解锁、main/tick 调度与共享冷却、失去模块后的能力变化、同时伤害、减速弹道与移动目标的精确相交、地形遮挡、射程耗尽、单发与持续射击差异、失败重试、暂停和重置、目标在最后允许 tick 达成优先于超时、敌人 JSON 校验与扩展字段保留，以及编辑器试玩不改写正式进度。持续 `shoot(0)` 的参考程序在第 28 tick（2.8 秒）通关，错误方向、单发及近战僵持均按预期失败。

另通过官方 Godot 4.5.1 WebGL 模板运行工程副本，目视检查中英文第四关编程页、敌方独立模块耐久、飞行中的弹丸、冷却与剩余时间、通关画面及第四关 SVG 缩略图。实际运行确认参考程序击毁两个敌方模块。视觉验证使用隔离数据目录，不写真实玩家存档；本轮未验证 Godot 4.7 原生 macOS 发布包。

写回 `/Users/mac/Desktop/ProgrammableModule` 后，在该实际项目路径重新完成 Godot 无窗口资源导入与第四关专项回归（112 项检查，0 失败），日志无引擎或脚本错误。写入前逐一核对原文件 SHA-256 并备份；其他原有项目文件保持不变。

## 2026-09-11：选关页四列纵向排列

选关页改为固定四列 GridContainer，更多关卡与末尾导入入口向下换行，仅启用纵向滚动，并支持焦点随滚动显现。图标按钮由 160×160 调整为 144×144；SVG 显示尺寸、内边距和圆角同步缩放，名称由 18 调整为 16（90% 的最近整数字号）。名称与说明均允许换行，避免长名称或英文文本撑开列宽。地图与 SVG 源纹理、排序、导入和玩法逻辑不变。

Godot 4.5.1 无窗口导入成功；现有游戏界面冒烟 265 项、两种尺寸与语言的布局检查 108 项，共 373 项检查，0 失败，最终日志无引擎或脚本错误。本轮为显示层小改动，未新增测试套件或重跑无关底层套件。

通过官方 Godot WebGL 导出副本目视检查：默认四关完整位于第一排，导入入口在第二排，无横向滚动条；隔离的 12 关预览在 1000×740 逻辑画布、英文及中英长名称下仍保持四列。实际向下滚动后可见第 9–12 关及末尾导入按钮，页头和底部说明保持固定。临时关卡及预览场景没有写入交付工程或正式存档。

## 2026-09-11：引导圆角与双向分页

引导弹窗保留 AcceptDialog 的原生标题、关闭和确认信号。嵌入模式下开启透明正文视口，并用保留 18px 内边距的 StyleBoxEmpty 代替重复绘制的正文卡片；完整圆角、白底与阴影统一由外层 embedded_border 绘制，消除标题接缝和底角灰色矩形。独立原生窗口模式继续使用普通正文背景。

底部左侧增加「上一页」，右侧「下一句」改为「下一页」；第一页禁用返回，末页仍可返回且右侧为「开始编辑」。重新打开引导从第一页开始；隐藏状态的分页回调不会继续推进。保留引擎按钮及占位节点，只调整弹性分布与主题按钮宽度，避免弹窗重新显示时恢复居中。新增 Previous Page / Next Page 英文翻译，长中文和无空格文本使用智能换行。

Godot 4.5.1 无窗口导入与现有游戏界面冒烟 265 项、布局回归 108 项均通过，共 373 项检查，0 失败，最终日志无引擎或脚本错误。通过官方 WebGL 导出副本目视检查第二关四页与第四关英文引导：完整圆角无重叠接缝，分页按钮在左右两端；实际点击下一页、上一页回到第一页、到达末页及开始编辑，确认正文和页码同步更新、第一页返回禁用、结束后回到原装配页。两种语言及 1000×740 逻辑画布下按钮与正文可见，第二关末页长段落完整换行。视觉副本使用隔离存档，未写正式玩家数据。

## 2026-09-11：引导关闭按钮与圆角同心

引导关闭按钮使用独立的 28×28 SVG，圆心 (14,14)、可见圆半径 12，提供浅灰默认与加深按下状态。位置读取实际 embedded_border 的右上圆角半径和扩展边距计算；当前外框半径 23、标题扩展 36，对应 close_h_offset=37、close_v_offset=27。按钮圆心距外框顶部和右侧均为 23，使内外圆弧同心。沿用 Godot 原关闭图标命中和关闭信号，不新增浮动按钮或自定义输入处理。

Godot 4.5.1 导入和 SVG 解析成功，既有游戏界面冒烟 265 项检查、0 失败，日志无引擎或脚本错误。通过官方 WebGL 导出副本在 1000×740 逻辑画布目视检查第四关引导：灰色圆形与外侧圆角留白对称，实际点击新圆心位置可关闭引导并回到原装配页。修改仅作用于嵌入式引导的局部主题；其他弹窗及原生窗口继续沿用既有样式。


## 2026-09-12：第五关「越狱行动」

按 ProgramLevel.md 加入三模块越狱关卡，警卫在左侧两格，警报器在右侧两格。新增逐关解锁的命名调用、警报守卫、方形安全门与实际到达出口的复合目标。警报处决在当帧全部伤害提交后判断，同 tick 同时清除两目标仍为有效解法。七页中英引导、命名提示、机关 SVG 和地图缩略图已配套。

macOS Godot 4.5.1 Standard 下执行 `tools/test.ps1`，十二套回归全部通过；之后仅补充非法关卡配置负例，单独重跑第五关套件通过。最终套件共 **1,970 项检查或断言，0 失败**：底层160、地图编辑器62、语言280、玩法239、设置99、游戏界面280、关卡与战斗237、弹道碰撞29、第四关112、监狱底层84、第五关264、布局124。最终日志无引擎、脚本、解析、编译错误或资源泄漏。

覆盖命名词法与小数兼容、真实接收者能力预检、整机移动、共享冷却、警报先毁处决、先警卫后警报、同帧近战与弹丸同时清除、关门阻挡、拆除后仍未离开不胜、暂停、重置、失败修正重试、错误行高亮、草稿保存及前四关权限。第五关负例包括非法 allow_named_calls、缺失/错类型/不匹配目标引用、不可行走出口，并核对失败不修改输入文档。

另用 JSON Schema 2020-12 校验全部五关、示例地图、模块和地块，以及新增字段负例，共19项通过。修复 Python 回归入口中过时且不存在的 combat_runner 条目，使其与 PowerShell 的实际套件一致。验证依赖仅在工作目录临时环境安装，未添加到游戏运行依赖。

使用官方 Godot 4.5.1 WebGL 模板运行隔离工程副本，目视检查中文引导与长段落、中文编程和通关、1000×740 逻辑画布下英文失败提示及引导，以及四列选关页的第五关 SVG。实际参考程序在50 tick（5.0秒）进入目标半径并胜利；错误顺序在第1 tick处决失败，完整英文提示可见。窗口翻页、圆角关闭按钮和缩略图正常。未验证 Godot 4.7 原生 macOS 发布包；未更改 project.godot 的版本标记和渲染设置。

原前四关 JSON 的 SHA-256 与任务开始时一致。正式存档未参与测试，预览场景和临时依赖不进入桌面项目。

写回 `/Users/mac/Desktop/ProgrammableModule` 后，该实际路径重新导入成功，第五关专项回归264项通过，日志无引擎或脚本错误。写入前核对源文件 SHA-256 并保存修改前项目备份；修改清单外的原文件内容保持一致。

## 2026-09-12：第六关「循环阶梯」

按 ProgramLevel.md 加入单模块的十组阶梯路线：每组向右 3 格、向上 2 格，共 51 个可行走方格，中心从 (1.5,21.5) 前往 (31.5,1.5)。第六关通过可选的 allow_loops 字段解锁 `loop {}`；循环持续执行，由到达终点结束本次运行。配套六页中英引导、代码高亮、自动生成的地图 SVG 缩略图和小尺寸地图适配。

macOS Godot 4.5.1 Standard 下执行 `tools/test.ps1`，十三套回归完整通过，共 **2,227 项检查或断言，0 失败**：底层160、地图编辑器62、语言364、玩法239、设置99、游戏界面300、关卡与战斗237、弹道碰撞29、第四关112、监狱底层84、第五关264、第六关137、布局140。最终日志无引擎、脚本、解析、编译错误或资源泄漏警告。

新增覆盖循环权限、嵌套执行与有界帧栈、空循环拒绝、tick 中循环拒绝、非法 AST 和循环引用保护、全程序能力预检、命名移动、零距离指令让出 tick、暂停续跑、取消重置、失败修正及错误行定位。修复循环块闭合但 main 缺少右花括号时的 EOF 诊断，使其正确提示缺少右花括号。真实参考程序在 498 tick（49.8 秒）进入目标半径并通关，不继续执行第十一组；九组展开程序在 450 tick 停在终点之前并失败。九组末点采用小于 0.001 格的绝对误差校验，容纳逐帧浮点累积误差，未改动移动或碰撞精度。

JSON Schema 2020-12 校验全部六关、示例地图、模块和地块，检查四份 Schema 及 allow_loops 类型负例、true/false 和默认省略，共 24 项通过。前五关 JSON 与本轮开始时逐字节一致。

官方 Godot 4.5.1 WebGL 模板运行隔离工程副本，在 1000×740 逻辑画布下目视检查十组阶梯完整显示、蓝色机器中心标记、循环语法高亮、中英通关状态和英文第 4 页代码引导。实际点击暂停后，位置与 40.2 秒计时保持不变；继续运行后在 49.8 秒通关。选关页第六关 SVG 显示完整阶梯路线，四列纵向布局正常。布局回归另覆盖 1280×800、1000×740 两种尺寸和中英两种语言的实际地图视口边界。

正式玩家存档未参与测试，视觉副本使用隔离目录；临时预览场景和校验依赖不写入桌面工程。本轮未验证 Godot 4.7 原生 macOS 发布包，未更改 project.godot 的版本标记或渲染设置。

写回 `/Users/mac/Desktop/ProgrammableModule` 后，在该实际项目路径重新完成无窗口资源导入和第六关专项回归（137 项检查，0 失败），日志无引擎、脚本错误或警告。写入前已核对原文件 SHA-256，并备份 197 个原有源码文件；29 个新增或修改文件在写回后再次核对，修改清单外的原文件保持一致。

## 2026-09-12：程序菜单本土化与重置代码

修复 CodeEdit 原生右键菜单的两处本土化缺口：补充 30 个英文源串的简体中文映射，并仅为内部 PopupMenu 显式启用自动翻译，不继承程序编辑区的禁翻译设置。保留 Godot 原生命令、快捷键、动态菜单重建、只读禁用与撤销栈，涵盖文字方向和控制字符子菜单；玩家代码仍保持原文。

在「重置位置」右侧加入「重置代码」。会话取消旧解释器、清空运行状态与错误行，恢复本关 starter_program，装配和通关记录保留。工作台将替换作为一次可撤销编辑，处理多光标及空模板，通过原有草稿事件自动保存。重新进入正常关卡显示重置后的程序；编辑器试玩仍不读写同 ID 正式存档，也不改动未保存地图及其撤销重做历史。第六关引导仅将“重置”明确为“重置位置”，路线与玩法规则不变。

macOS Godot 4.5.1 Standard 下完整执行 tools/test.ps1，十三套共 2,360 项检查，0 失败。随后针对实际预览中撤销后提示未更新的问题补充刷新逻辑和一项断言，重新运行游戏界面套件 364 项通过。最终各套件合计 **2,361 项检查或断言，0 失败**：底层160、地图编辑器62、语言364、玩法239、设置141、游戏界面364、关卡与战斗237、弹道碰撞29、第四关112、监狱底层84、第五关264、第六关137、布局168。最终日志无引擎、解析、编译、脚本错误或资源泄漏警告。透明底色断言按 alpha 为零校验，以兼容 Godot 原生新行返回透明黑而 Color.TRANSPARENT 为透明白的差异。

新增测试通过真实右键输入检查主菜单及子菜单的中英切换、只读状态、命令 ID 与快捷键不变；通过实际按钮回调和自动保存计时器验证重置、原子撤销重做、运行与暂停取消、错误恢复、重进关卡、空初始程序、多光标、其他关卡隔离和编辑器试玩隔离。两种语言及 1280×800、1000×740 尺寸下检查全部工具栏按钮可见。

使用官方 Godot 4.5.1 WebGL 模板运行隔离工程，实际右键确认中文主菜单和文字方向子菜单；点击重置恢复第六关初始模板，并用 Command+Z 找回原程序。预览与测试使用独立存档目录，正式玩家数据未参与。未验证 Godot 4.7 原生 macOS 发布包；未更改 project.godot 或渲染设置。

写回 `/Users/mac/Desktop/ProgrammableModule` 后，在实际工程路径重新导入并通过设置141项、游戏界面364项、布局168项，共673项相关检查，日志无错误或警告。中英文 WebGL 预览均确认按钮完整可见、菜单对应语言正确，撤销后状态提示同步更新。写入前备份201个原有源码文件；12个修改文件已逐一核对 SHA-256，其他原有文件保持一致。

## 2026-09-12：第六关改为玩家自行补全循环

初始代码只保留 main() 与空 loop 框架，移除预写移动及答案注释。第六关描述、第2/4页引导和编程帮助改为观察重复规律、转弯和路段长度，并同步英文，不直接给出步长或完整解法。保持六页引导、实际路线、装配限制、通关条件和解释器规则；空循环未补全时仍提示至少添加一条动作指令。已有玩家草稿不做迁移，点击「重置代码」可恢复新框架。

macOS Godot 4.5.1 无窗口导入及第六关137项、游戏界面364项、两语言两尺寸布局168项，共669项检查通过，日志无引擎或脚本错误、警告。同步修改已有模板编译断言，以符合需要玩家填写的初始状态；未增加新的测试套件或修改核心执行逻辑。JSON 比对确认除描述、引导与初始程序外，第六关全部数据相同；本轮为文案与模板调整，未重跑无关底层测试，也未重复导出图形预览。

写回桌面项目后重新导入并通过第六关137项检查，日志无错误或警告。写入前已备份源码，六个修改文件最终 SHA-256 一致，其他原有文件不变。

## 2026-09-12：第七关「边退边射」

按 ProgramLevel.md 第七关加入两模块上限、一格宽水平通道、右侧6格持续向左推进并近战的敌人，以及逐关开放的 `if / else` 和具名 `ready()`。初始代码只保留空 main / loop 框架，条件与动作由玩家自行填写。第七关的射击冷却提示使用与 ready 相同的下一动作 tick，超时和 main 结束提示不再套用第四关原地射击建议；前六关 JSON 保持逐字节一致。

macOS Godot 4.5.1 Standard 与 PowerShell 7.6.6 完整执行 `tools/test.ps1`，十五套共 **2,872 项检查或断言，0 失败**：底层160、地图编辑器62、语言530、玩法239、设置141、游戏界面412、关卡与战斗237、弹道碰撞29、第四关112、监狱底层84、第五关264、第六关137、追击底层125、第七关156、布局184。检查退出码、完成标记及引擎日志，最终无引擎、解析、编译、脚本错误或资源泄漏警告。中文引导分段微调后，重新通过第七关156项、界面412项与布局184项，共752项相关检查。

四份 JSON Schema 自身合法；12份实际内容和13个新增配置负例共25项校验通过。就绪纯查询覆盖下一 tick 冷却边界、具名隔离及已失效模块；条件覆盖真假路径预检、混合结构深度、坏AST、取消与重入。追击覆盖持续移动与攻击并存、地形阻挡、驱动失效、同tick伤害及模块耐久覆盖的实例隔离。实际平衡探针确认短距离后退的多种解法通关，而原地单枪、原地双枪、仅后退均不能通关。

用官方 Godot 4.5.1 WebGL 模板运行隔离预览，目视检查1000×740逻辑画布下的中英文编程页、空循环模板、条件语法高亮、中文引导的上一页与圆形关闭按钮。测试/预览使用独占存档目录，不读写正式玩家的代码与通关记录；未验证 Godot 4.7 原生 macOS 发布包，也未修改 project.godot、渲染配置或全局模块定义。

WebGL 交互复核：在0.3秒暂停后，位置、弹丸与剩余29.7秒保持不变；继续后在9.6秒击毁全部敌方模块并通关。点击英文 Reset Code 恢复空循环，Command+Z 恢复原程序；英文第4页条件引导、上一页/下一页及圆形关闭按钮可正常操作。选关页显示七个SVG关卡图标与导入入口，沿用四列纵向布局。

写回桌面项目之前备份201个原有源码文件，并检查所有原文件SHA-256以避免覆盖并发编辑；同步32个新增或修改文件。写回后在实际桌面路径重新无窗口导入，并通过语言530项、追击底层125项、第七关156项与游戏界面412项，共1,223项相关检查，日志无错误或警告。逐字节核对前六关、项目配置、模块定义及修改清单外的原文件均保持一致。

## 2026-09-15：第十一关「避实击虚」

环境：macOS，隔离工程与 Godot 4.5.1 stable。标准 `tools/test.ps1` 的 34 组检查全部覆盖，最终合计 10870 项断言或检查、0 失败。首轮停在游戏 UI 冒烟中已过时的指令总数断言；更新为包含 functions 的 16 项后，从该组继续运行标准列表余下各组，不重复统计此前通过的组。Godot 最终导入成功，最终各组没有引擎或脚本错误。

新增四组：函数语言 126 项、第十一关 171 项、雷达突袭底层 86 项、函数教学界面 42 项，全部通过。覆盖前向引用、无参数函数调用、嵌套返回、拒绝递归/未声明函数/未解锁语法、函数内能力校验、暂停与重试；真实五次落空才通关、站桩首次受击失败、移动完整占地、雷达范围与地形遮挡、锁定后不重新瞄准、取消攻击后恢复、模块损毁和冷却不产生虚假计数。其余 30 组验证旧语言、前九关、存档清除、搜索、补全、悬浮提示及普通/全尺寸布局。

内置目录为第一至第九关与第十一关共 10 项，第十关无占位；已确认前九关 JSON 保持原文。此次函数初始交付仅新增函数权限；后续常变量与代码提示扩展见下节；该次初始交付时玩家 scan() 未开放，当前雷达扩展见本文最后一节。移动后的装配重叠检查改用局部坐标，避免浮点平移把相邻共边模块误判为重叠，世界地形检查仍使用真实世界占地。

已导出隔离 Web 预览并目视检查中文空函数初始代码、首次七页引导中的首屏、英文运行/暂停时的闪避次数，以及全尺寸成功药丸；关卡标题和新 SVG 雷达模块可见，画面无重叠裁切。参考解法只存在于测试与开发文档，游戏初始程序保留空 main/function 框架。检查使用独立 user://tests 目录，不修改玩家设置、进度或草稿。38 个 data/schema JSON 语法通过；动态引用由真实游戏校验器及上述测试验证，未运行外部 Schema 验证器。

## 2026-09-15：常变量与教学代码提示

本轮将第十一关的 `allow_variables` 显式开启，继承关卡文档中的第十关数值绑定能力；第十关地图仍不提供，前九关保持原权限。语言范围及运行预算见 [常量与变量](VARIABLES.md)，逐步提示与存储约定见 [代码提示](CODE_HINTS.md)。

验证范围包括常量与变量声明、赋值、词法作用域、函数隔离、初始化先后、常量写保护、回调和并行只读规则，以及补全的权限、注释与可见名称边界；指令集合覆盖中英资料与独立解锁。设置检查无 / 一般 / 多、默认一般、旧配置迁移、Tab 补全独立性和坏文件原始字节保护。

教学提示检查正式来源与完整地图限制、实际装配和参考方案的独立通关验证、每次只补一步、定位高亮与相邻 SVG 撤销。撤销只处理历史顶部的最新提示，不覆盖后续手工编辑。会话与存档检查每次失败只计一次、按关跨会话恢复、成功清零、停止 / 重置不增加失败、旧记录兼容及损坏记录保护；导入与编辑器测试不提供提示或写提示计数。验证使用独占测试目录，不读写正式玩家作品。

本轮验证使用 Godot 4.5.1：`tools/test.ps1` 共 38 组回归、11,705 项检查/断言通过，0 失败，无脚本或引擎错误。随后针对撤销同帧同步及按钮文字避让补测：代码提示界面 54 项、行内补全 87 项、游戏界面冒烟 1,068 项全部通过。已检查实际 Web 导出画面中的设置三档、提示与撤销 SVG 位置。测试存档均使用独立 `user://tests` 子目录。


## 2026-09-15：第十一关直接开放玩家雷达

本轮按用户最新要求开放第十一关的雷达模块与 `allow_radar`，保留常变量权限、两模块上限、原有测距方案及五次闪避目标。第十关地图仍跳过，前九关权限不变。组装引导、模块说明、指令集合、补全和代码提示同步说明玩家已可使用雷达。

验证入口包括 `tests/player_radar_world_runner.gd`、`tests/radar_language_runner.gd` 与 `tests/player_radar_level_runner.gd`，分别覆盖真实雷达来源与最近可见目标、快照和空目标语法、正式关卡权限与可运行方案。既有 `tests/radar_lunge_world_runner.gd` 和 `tests/rangefinder_world_runner.gd` 用于检查敌方锁定行为与测距几何未被雷达扩展改变；指令目录、补全、代码提示及游戏界面继续由对应标准回归覆盖。

本轮在 macOS 隔离工程上使用 Godot 4.5.1 运行标准 `tools/test.ps1`：41 组、12,160 项检查/断言全部通过，日志无引擎错误、脚本错误或警告。随后修复 `Null` 模块名与空值关键字冲突，补测玩家雷达语言 155 项、旧语言 530 项、游戏会话 239 项、补全 391 项、雷达教学 30 项，全部通过；这些补测与全量中同组重叠，不重复累计。新增来源选择、最近可见目标、遮挡、快照复制及纯查询测试覆盖 81 项；正式第十一关的两种雷达/移动装配都真实完成五次闪避。测距和雷达两份提示方案分别验证可通关，保留玩家函数名、注释和逐步插入规则。

隔离 WebGL 导出成功，并目视检查首次引导、雷达目录的实际选中、指令集合正常黑色显示及展开说明，以及雷达程序成功通关画面。第十一关仍为两模块上限与五次闪避，首个 main/function 框架保留空内容。前九关 JSON 与本轮开始前逐字节一致，不产生第十关地图。55 份 data/schema JSON 语法检查通过，所有改动 GDScript 的函数均有用途注释；未运行外部 JSON Schema 验证器。测试和预览均使用独立测试存档，不读写真实玩家设置、进度或作品。

## 2026-09-15：代码区颜色与设置滚动条

设置「显示」新增「颜色与显示」子页，真实只读 CodeEdit 缩略图可选择浅色或深色；默认浅色，深色背景固定为 `#121314`。模式写入独立设置字段，保留旧配置与未知扩展，写入失败后可再次点击当前模式重试。工作台仅更新本地主题，覆盖语法、行号、选区、光标、Tab 灰字与错误/执行行，不改玩家代码、撤销栈、运行状态或外围页面配色。

macOS 隔离工程使用 Godot 4.5.1 完整运行标准 `tools/test.ps1`：44 组、12,756 项检查或断言全部通过，0 失败；日志无引擎、解析、脚本错误或警告。新增代码颜色设置 126 项、编辑器 276 项、界面 181 项已包含在全量中。随后按用户要求增加滚动条右侧间距与闲置淡出，并补充保存失败重试及完整缩略图检查，再次通过颜色界面 227 项、设置 249 项、布局 3,261 项；这些专项和全量中的同组重叠，不重复累计。

滚动条保留原生占位，仅改变透明度：初始及闲置隐藏，实际滚动显示，停止约 0.8 秒后用 0.2 秒淡出，拖住期间保持显示。回归覆盖滚轮、触控板、原生键盘滚动、拖动及条外松开、悬停不触发、离页释放；中英文与 1280×800、1000×740 布局中卡片宽度及右侧 24 像素间距保持稳定。缩略图完整显示六行，无内部滚动条；预览点击不编辑代码。

隔离 WebGL 预览已目视核对设置入口、两种缩略图和选中切换、真实深色工作台，以及滚动时出现和停止后隐藏的滚动条。代码与界面使用独立测试存档目录，不读写真实玩家设置、进度或作品；临时场景、导出与探针不进入桌面工程。所有关卡 JSON、模块定义、地图及 project.godot 保持本轮开始时的原文。

## 2026-09-15：深色提示按钮

代码编辑区选择深色时，代码提示与相邻撤销按钮改为深灰半透明玻璃圆底及白色轮廓 SVG；浅色恢复原资源。背景仅在按钮附近进行 9 点模糊，图标由原生按钮单独绘制；全尺寸过渡和隐藏时停用背景复制，避免影响整卡淡出。未改按钮尺寸、提示权限或撤销事务。

Godot 4.5.1 隔离工程资源导入成功；既有代码配色编辑器回归 387 项、代码提示界面回归 54 项全部通过，0 失败，日志无引擎或脚本错误。覆盖初始即深色、深浅往返、实际提示插入与撤销、关闭提示及全尺寸过渡期间采样启停；继续验证源码、光标、选区、撤销栈和运行状态保留。WebGL 预览核对了深色灯泡、提示后出现的撤销按钮和全尺寸展开/收回，按钮及代码无残影。所有测试和预览采用独立测试目录，不读取或修改正式玩家存档。

## 2026-09-16：第十关“猎人游戏”

环境：macOS、Godot 4.5.1，独立阶段目录及测试存档。完成第十关随机追击复习，按用户最终名称使用“猎人游戏”；第一至第十一关按真实 order 排序。

- `tools/test.ps1` 完整 45 组回归通过，脚本/解析/引擎错误日志为零。新增追猎回归 446 项，提示 432 项、指令资料 598 项、关卡搜索 669 项、布局 3285 项均为零失败。
- 第十关以 64 种固定测试种子分别执行无绑定及 value/variable 两种写法，共 128 次真实通关，最慢 40.6 秒；敌人必须由普通弹道和伤害流程真实击毁。测试种子不进入正式地图。
- 覆盖随机路线独立性与重放、真实速度、地图边界、void 与阻挡物、18 点实例耐久隔离、驱动损坏后停止、三模块上限、暂停恢复、120 秒超时、重置保留作品及提示副本隔离。
- 目录初次扩展后布局用例的旧导入索引与十关计数已更新为十一关；修复后的完整回归全部通过。最终另修正第十关选关副标题，避免沿用第九关的测距说明；导入、游戏界面冒烟 1079 项和布局 3285 项再次通过，无脚本错误。
- 实际 Web 预览检查选关、工作台、运行中耐久/冷却药丸、全尺寸展开、随机追击通关反馈及重置收回。名称、七页指引和追击提示提供中英翻译。

正式玩家草稿、进度和设置未用于测试；同步前检查桌面工程与阶段基线一致，保留被替换文件备份。

## 2026-09-22：开始菜单背景设置

「颜色与显示」缩小代码配色预览，并加入默认图片、原有纯色与自定义背景选项。简化缩略图表示白色菜单与图片的关系，实际图片背景沿用约 64 逻辑像素高斯模糊。图库四张一屏，溢出后使用不自动淡出的横向原生滚动条；新图片自动滚入可视区，未使用的自定义图片支持紧贴点击位置的小型右键删除菜单，删除不影响原始照片。

Godot 4.5.1 的隔离工程完整运行 `tools/test.ps1`，70 组全部通过，日志无引擎、脚本、解析错误或警告。新增模型回归 110 项，覆盖旧配置缺省、独立图片副本、六张背景、切换重载、文件丢失、路径校验、当前背景删除保护及保存失败回滚。新增 UI 回归覆盖真实选项点击、文件选择信号、横向滚动及静置常显、右键删除与原图保留、中英两种尺寸的布局；对最后调整的自动滚入和事件坐标定位另行复测，最终 UI 专项 86 项全部通过。

实际 GPU 画面检查了 1280×800 的初始设置、六张图库、右键菜单、纯色和自定义开始页，以及 1000×740 的英文设置布局。六行代码缩略图无内部滚动条，小窗口图库不被截断。所有测试使用隔离设置与图片目录，未读取或更改正式玩家的背景偏好、进度或草稿。


## 2026-09-22：香港繁体与地区语言排序

设置的显示语言固定为「简体中文（中国大陆）」「繁體中文（中國香港）」「English (US)」，以各自语言自名显示。香港静态词典覆盖 929 个界面源串、30 个原生编辑菜单源串及 27 份指令资料的 81 个显示字段；格式占位符和语法标识符保留。旧英文配置仍保存为 `en`，地区名称与排序不会改变已有选择。

隔离工程使用 Godot 4.5.1 完成标准 `tools/test.ps1` 的全部 72 组检查。期间用户追加排序要求，旧界面冒烟中的固定英文索引因此失效；改成按语言代码选择后，设置 303 项、香港繁体 4401 项、游戏界面 1200 项重新通过，并使用原包装函数完成剩余 23 组，最终没有未通过的组。成功日志无脚本、解析、编译错误或引擎警告。

香港专项检查设置保存和跨模型重载、三语言连续切换、旧 `en` 兼容、静态译文覆盖与占位符顺序、全部繁体字形、原生编辑菜单命令/快捷键/撤销、指令搜索及香港 locale 别名、动态错误在切换后的模板缓存、玩家文本与 JSON 载荷保持原文，以及早期启动失败读取已保存语言。按游戏最小 1000×740 窗口真实打开语言弹层，修正新增选项造成的底部越界，保持 8 像素边距。

另用原生 GPU 在 1280×800 和 1000×740 检查三个语言的设置、香港主菜单与展开的指令说明；排序调整后重拍两种尺寸的香港设置，确认简体第一、繁体第二、英语第三，文字完整、圆角无裁切。测试使用独立设置与用户目录，不修改玩家原设置、地图、代码或进度。


## 2026-09-22：两级设置移除外卡、语言简称与背景单排追加

显示语言改为「简体中文」「繁體中文」「English」，顺序、语言代码及翻译内容保留。设置主页面和「颜色与显示」子页移除外层白色卡片，各分组直接显示在浅色背景；滚动到返回药丸底边时以 48 像素 alpha 渐变淡出。背景图库按用户追加反馈改为默认、纯色、自定义添加入口、全部自定义图片共用一排，新增仅向右追加；溢出时横条常显，导入后自动滚到新图。

本轮按 UI 范围运行 Godot 4.5.1 专项与关联回归：设置 303 项、香港繁体 4401 项、代码颜色界面 227 项、菜单背景界面 146 项、游戏界面冒烟 1200 项、布局 3545 项、新增设置开放布局 246 项，均为 0 失败。新增设置回归已纳入 `tools/test.ps1`；本轮没有重复宣称执行全部玩法套件。

原生 GPU 验证设置开放布局 304 项、菜单背景 146 项，均为 0 失败，无引擎、脚本、解析错误。覆盖三种语言 × 1280×800 / 1000×740：设置滚动、真实点击与键盘焦点、页间返回、颜色预览、同排图库首尾横滚、常显横条、导入与删除。删除菜单扣除原生窗口双侧阴影预算，三语言下含阴影的宽高仍不超过预览一半。设置的 CanvasGroup 使用完整 alpha 精度后，GPU 圆角无暗点；蓝/绿底图对照证明渐隐是透出底色而非白色覆盖，两个页面均有连续中间透明度，邻接差异最大 0.020。

使用隔离工程、独占测试设置及生成图片，退出清理测试数据并恢复全局音量/语言。正式玩家图片、设置、地图、草稿及进度未用于测试。交付前按基线哈希检查正式工程，保留被替换源文件备份。


## 2026-09-22：恢复设置分组灰底的视觉层次

移除外白卡后原分组色 `#F2F3F5` 与页面 `#F3F5F9` 过于接近；五处分组统一使用 `GameTheme.SETTINGS_GROUP_BACKGROUND`（`#E5E7EB`），组内分隔线调整为 `#D0D5DD`。组内透明行、顶部 alpha 渐隐、代码预览、图片缩略图及交互逻辑保持原实现。

现有设置开放布局回归 headless 246 项、GPU 304 项全部通过，覆盖三语言、两种分辨率、同排图库和两页渐隐。实际截图确认音量、显示、更多、代码颜色、背景选择各组的圆角和灰底清晰可辨。首次 GPU 执行首组原生弹层键盘选择出现 4 项连锁失败；未更改产品逻辑或断言，串行重新执行后通过，保留首次及最终日志。测试使用原隔离工程与专属测试数据，未改玩家偏好。


## 2026-09-23：关于游戏作者与可点击联系方式

设置「更多」首行新增「关于」，打开居中半透明玻璃竖卡，包含游戏图标、标题、2026–2027、作者与联系方式。网站、Bilibili、YouTube、GitHub、邮箱使用真实 LinkButton；仅明确点击或键盘激活时提交固定地址给系统，失败就地反馈。外部点击、Esc、关闭按钮均返回当前设置并恢复入口焦点，页面切换清理弹窗，不穿透底层设置或与清除弹窗叠开。

Godot 4.5.1 隔离导入无脚本或引擎错误。新增 `about_game_runner.gd` headless 551 项、GPU 563 项全部通过，使用真实鼠标和键盘、系统打开替身及独占数据目录。覆盖三语言 × 1280×800 / 1000×740 的入口顺序、作者文字与精确链接、无自动跳转、五链接激活、失败反馈、模态输入、焦点循环/恢复、缩放与切页关闭。最后调整图标至 104 像素并隐藏鼠标焦点描边后重新运行 GPU，563 项仍全部通过，12 张最终正常/失败截图均无裁切或重叠。

关联设置开放布局 246 项、繁体语言 4417 项、整体布局 3545 项均为 0 失败；既有定位无需修改。新增 runner 已纳入 `tools/test.ps1`。本轮属于设置 UI 范围，没有重新运行或宣称完整玩法套件。测试不启动真实浏览器或邮件程序，不发送邮件、不改变正式玩家偏好、照片、地图、代码或进度。


## 2026-09-23：装配图上缘复用设置页透明渐隐

自由查看模式将图纸、模块、选中框和拖动预览先合成，再统一应用顶部 48 像素 smoothstep。固定图纸保留原直接绘制与完整白框；设置页与装配页共用按视口引用计数的完整 alpha 精度管理。

Godot 4.5.1 隔离导入通过；新增装配顶部回归 headless 25 项、GPU 83 项，原装配视图 82 项、设置开放布局 246 项，均为 0 失败。GPU 使用 OpenGL Compatibility，在 1280×800 与 1000×740 下通过真实点击放置、滚轮缩放、右键拖图和左键预览，再切换蓝/绿父卡底色反推透明度。顶部 1/24/48 像素的 alpha 约为 0/0.516/1，与真实设置行的曲线最大差为 0.0095；纸张、模块、选框与虚影之间最大差为 0.0101，说明重叠内容只淡出一次。画面保留锐利格线，边缘透出实际底色，无白雾覆盖。

固定模式四边白框实际可见；模式切换与返回页面恢复视口状态，查看操作不改变模块数据、模型信号或草稿文件。最终截图覆盖两种尺寸的顶部模块、拖动虚影及固定图纸。测试仅使用独占数据目录，新 runner 已加入统一测试入口；本轮未重复运行完整玩法套件。


## 2026-09-23：地图空白区域使用浅蓝灰石纹

参考用户提供的石材图，用内置图像生成工具制作低对比度纹理；资源保存至 assets/tiles/void_stone.png（1254×1254），启用 mipmap 导入。MapCanvas 只新增一次背景纹理绘制，均色校正后以 65% 强度混入原浅蓝灰底色；16 格连续映射并镜像重复，避免逐格重复与边缘接缝。未修改地块定义、地图、世界模拟或编辑文档。

Godot 4.5.1 隔离导入通过；现有地图编辑器视口回归 132 项、全尺寸预览回归 412 项均为 0 失败。原生 OpenGL Compatibility 截图检查第六关常规/全尺寸界面、1280×800 与 1600×1000，以及编辑器原尺寸和 2.5 倍缩放：浅蓝灰底色保持接近原界面，细石纹可见，重复边缘无硬接缝，白路、目标和机器未被染色。截图及试运行仅使用隔离工程与测试目录；本次纯视觉改动未新增镜像实现测试，未运行完整玩法套件。


## 2026-09-23：通行地板使用提亮划痕材质

将用户提供的细划痕图原样打包为 floor_scratched.png，渲染时提亮并以 35% 混合在原白色地板圆角内侧。按地图坐标连续取样，保留外框，避免每格重复；仅用于原生 floor.svg，其他地块、地图及碰撞规则不变。资源开启 mipmap，使用独立纹理滤波。

Godot 4.5.1 隔离导入通过；现有地图编辑器视口回归 132 项、全尺寸预览回归 412 项均为 0 失败。GPU 检查第一关正常/全尺寸地图、1280×800 与 1600×1000，以及编辑器原尺寸和 2.5 倍缩放：划痕可见，通行区域亮于周围浅蓝灰石纹，圆角外部无方形纹理溢出，网格、机器、终点与编辑输入保持正常。截图使用隔离数据目录；纯视觉变更未新增镜像实现测试，未运行完整玩法套件。


## 2026-09-23：放大时的地图纹理清晰度

使用内置图像生成工具重绘两套纹理，实际输出均为 1254×1254，未插值放大。每 8 格映射取代此前的 16 格映射，使每格约 157 个纹理像素覆盖编辑器最高 4 倍缩放的 128 逻辑像素；继续使用 mipmap 控制缩小时的细节。石纹重新校正均色并以 45% 混合保留浅蓝灰外观；通行路面划痕从 35% 降至 24% 混合，保持明亮并降低对比度。原生地板 SVG 边框以 4 倍分辨率绘制，原始源文件和导入资源路径使用一致的 CanvasTexture mipmap 滤波。

Godot 4.5.1 隔离导入通过，地图编辑器视口 132 项、全尺寸预览 412 项检查均为 0 失败。OpenGL Compatibility 在相同视点捕获更改前后 1、2.5、4 倍编辑器画面，以及第六关全尺寸地图；目视确认放大后的细石纹与浅划痕清楚、圆角边框平滑，正常地图仍保留明亮道路与周围浅蓝灰石纹的区别。独立读取探针分别验证原始 SVG 存在和仅有导入资源两条路径：均为 256×256、含 mipmap、使用相同过滤，缓存复用及清理后重新读取正常。最终导入、回归、截图和读取日志无错误或警告。

所有检查仅使用隔离项目和测试存储；不修改地块数据、编辑操作或游戏规则。本轮属于资源与呈现调整，未新增长期样式断言，也未重复运行完整玩法套件。


## 2026-09-23：通行格子边界减淡

仅调整原生 floor.svg：圆角描边不透明度设为 0.75，格缝底色相对 #FBFCFE 的色差降至约 75%（#EBEFF6 → #EFF2F8）。描边宽度、圆角几何、内部角标、细划痕和地图网格绘制保持原值。

Godot 4.5.1 隔离导入无错误；OpenGL Compatibility 实际捕获编辑器 1、2.5、4 倍缩放与第六关全尺寸地图。目视检查最大缩放和全图：格子轮廓更淡，通行路径仍清楚可辨。截图日志无错误或警告；本次仅 SVG 样式调整，没有运行或新增玩法测试。测试使用隔离工程与独立存储目录。


## 2026-09-23：独立应用图标与导出配置

将现有 workshop.svg 原样复制为 assets/app/app_icon.svg，并逐尺寸直接栅格化生成 1024px PNG、macOS ICNS 与 Windows ICO。验证所有 PNG 尺寸与透明角、SVG 字节一致性、ICO 七个目录项及 ICNS 的 iconutil 反解；实际查看图标。project.godot 设置通用与原生图标，已有 macOS / Windows Desktop 导出预设分别填写 ICNS / ICO，保留用户正在调整的其他导出设置。

Godot 4.7.2 Mono 隔离导入通过。首次按现有 Distribution 配置导出受到未完成的公证/签名设置阻止；随后只在隔离副本临时使用 Testing 分发、内置 ad-hoc 签名和关闭公证，以 --export-release 成功生成 macOS ZIP。Info.plist 指向的 Resources/icon.icns 与生成文件字节一致，打包日志包含两种原生图标和通用 PNG；codesign --verify --deep --strict 验证通过。测试配置未同步回正式项目。发布二进制的启动探针被本机系统终止（137），因此不宣称验证了应用启动或实际标题显示；Release 不附加 DEBUG 的结论依据 Godot 官方窗口实现。当前只安装了 macOS 模板，未进行 Windows EXE 导出或 Windows 运行测试。

本次不修改游戏脚本、玩法数据或玩家设置，也不将验证导出包、临时目录和引擎缓存同步至工程。原始 UI 图标未改动。生成工具可重复运行；图标更新无需重新绘制游戏界面资源。
